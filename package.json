{
    "name": "kaplay2024",
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "preview": "vite preview",
        "zip": "yarn run build && mkdir -p dist && zip -r dist/game.zip www -x \"**/.DS_Store\"",
        "check": "tsc"
    },
    "dependencies": {
        "kaplay": "^3001.0.2"
    },
    "devDependencies": {
        "typescript": "^5.6.3",
        "vite": "^5.4.10",
        "prettier": "^3.3.3"
    }
}
